  CREATE TABLE PMS_ROLES(
       NAME VARCHAR(30) NOT NULL PRIMARY KEY,
       ACCESS TEXT NOT NULL,
       CREATED_BY VARCHAR(30) NOT NULL,
       CREATED_ON TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
 )ENGINE=InnoDB;

  CREATE TABLE PMS_USERS(
    USERNAME VARCHAR(30) NOT NULL PRIMARY KEY,
    FIRSTNAME VARCHAR(255) NOT NULL,
    LASTNAME VARCHAR(255) NOT NULL,
    ADMIN_USER BOOLEAN,
    PASSWORD TEXT NOT NULL,
    SALT_SECRET TEXT NOT NULL,
    ROLE VARCHAR(30),
    CREATED_BY VARCHAR(30) NOT NULL,
    CREATED_ON TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UPDATED_BY VARCHAR(30) NOT NULL,
    UPDATED_ON TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY(role) REFERENCES PMS_ROLES(name) ON UPDATE CASCADE ON DELETE RESTRICT
  )ENGINE=InnoDB;




INSERT INTO PMS_ROLES (NAME,ACCESS,CREATED_BY) VALUES ('ROLE_ADMIN','ALL','SUPER_ADMIN');
INSERT INTO PMS_ROLES (NAME,ACCESS,CREATED_BY) VALUES ('ROLE_USER','ALL_READ','SUPER_ADMIN');


